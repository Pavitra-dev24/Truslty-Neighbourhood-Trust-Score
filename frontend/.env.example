# Copy this file to .env for local development (Vite auto-loads .env files).
# Never commit your real .env file.

# Points at your backend. Locally this is your uvicorn server;
# after deploying the backend to Render, use its https URL instead.
VITE_API_BASE_URL=http://127.0.0.1:8000
